#about.content
  %span.actions
    - if can? :update, @page
      = link_to t(:change_nav).html_safe, edit_admin_core_path(@page), :remote => true
  - cache(@page) do
    = markdown(@page.content).html_safe
