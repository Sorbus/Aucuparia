$('#yield').html('#{escape_javascript(render partial: 'profiles/show' )}');
- cache(@user) do
  var title = '#{t(:view_user_title, :user => @user.display_name)}'
  history.pushState( null, title, '#{user_path(params[:id])}' )
