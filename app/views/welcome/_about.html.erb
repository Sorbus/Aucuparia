<div class="content" id="about">
	<span class="actions">
		<% if can? :update, @page %>
			<%= link_to '<i class="fa fa-pencil-square-o fa-lg"></i>'.html_safe, edit_admin_core_path(@page) %>
		<% end %>
	</span>
	<%= markdown(@page.content).html_safe %>
	<br>
	<div class="navigation">
		<%= link_to 'All Categories', categories_path, :remote => true %>
		<span class="tab"></span>
		<%= link_to 'All Items', items_path, :remote => true %>
	</div>
</div>