.content.narrow
  %p.definition
    = t(:index_definition).html_safe
  %hr/
  .tags
    - @tags.each do |tag|
      .tag= link_to tag, tag_path(tag.name), :remote => true
  %hr/
  = paginate @tags, :remote => true
