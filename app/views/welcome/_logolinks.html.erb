<% if Core.first.show_icons? %>
	<div id="linkbar">
		<% @links.each do |link| %>
			<% if link.display? %>
				<%= link_to '&nbsp'.html_safe, link.url, :id => link.css_id, :class => link.css_class %>
			<% end %>
		<% end %>
	</div>
<% end %>