<% content_for :head do %>
	<title><%= @page.title %></title>
<% end %>

<div class="content" id="about">
	<span class="actions">
	<%= link_to_if(can?(:update, @page), link_to 'Change', edit_admin_core_path(@page)) %>
	</span>
	<%= markdown(@page.content).html_safe %>
	<br>
	<div class="navigation">
		<a href="/categories">All Categories</a><span class="tab"></span><a href="/items">All Items</a>
	</div>

</div>