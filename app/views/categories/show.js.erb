$('#yield').html('<%= escape_javascript(render partial: 'show' ) %>');
var title = '<%= (t :view_category_title_before) + @category.name + (t :view_category_title_after) %>' 
history.pushState( null, title, '<%= category_path(params[:id], params[:page]) %>' )
