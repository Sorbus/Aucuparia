<% content_for :head do %>
	<title>Creation</title>
<% end %>

<div id="content">
	<p id="definition">
		<strong>Creation</strong> - <em>n</em>. The act of causing something that does not exist to exist.
	</p>
	<%= render 'form' %>
</div>
<% if @item.content.present? %>
	<div id="content">
		<%= markdown(@item.content).html_safe =%>
	</div>
<% end %>