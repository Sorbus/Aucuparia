<div class="content">
	<p class="definition">
		<%= t(:creation_definition).html_safe %>
	</p>
	<%= render 'form' %>
</div>
<% if @item.content.present? %>
	<div class="content">
		<%= markdown(@item.content).html_safe =%>
	</div>
<% end %>